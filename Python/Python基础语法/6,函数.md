# 特性
可返回多个元素，其实质是返回一个元组
return x,y  即(x,y)

函数内修改全局变量：global 全局变量名  （要先声名再使用） 

# 参数
传参方式：
	位置传参，关键字传参

默认参数   def function(x,y,z = a)

不定长参数
	基于位置参数：def function(\*args)
		args在函数内被封装为元组
	基于关键字参数：def function(\*\*kwargs)
		kwargs在函数内被封装为字典

函数也可以作为参数

# 匿名函数
lambda  参数列表:函数体
	函数体只能是一行

