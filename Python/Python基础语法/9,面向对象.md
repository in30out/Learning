# 类的命名规范
	大驼峰命名法，每个单词的首字母都是大写，单词之间没有分隔符 例：UserInfo
\_\_dict\_\_是python中用户自定义类实例的一个特殊属性，用于以字典形式存储对象属性

# 魔法方法
魔法方法是指Python中提供的双下划线开头和结尾的特殊方法，用于定义类的特殊行为，比如：\_\_init\_\_
魔法方法不需要手动调用，Python会在合适的时机自动调用

### 常用魔法方法
```
__init__ 初始化方法
_\_str\_\_ 字符串表示的方法
_\_eq\_\_ 比较两个对象是否相等
\_\_lt\_\_（小于（less than））
\_\_le\_\_(小于等于（less than or equal))
\_\_gt\_\_(大于（greater than）)
\_\_ge\_\_(大于等于（greater than or equal）)
类名.__dict__       为类的所有属性
类名.__mro__        为类的方法解析顺序        MRO:Method Resolution Order
```
# 面向对象高级
## 封装
```
#私有化变量
__变量名

#私有化方法
def __方法名

#强行访问私有化变量和方法
_类名__私有化名
```

## 继承
```
class 子类(父类):
	pass
	
#调用父类方法
方式一：super().方法名()
方式二：父类名.方法名(self)

#多继承
#当一个类继承了多个父类时，默认优先使用第一个父类中的同名属性或方法，可以使用  类名.__mro__   或    类名.mro()方法查看调用顺序。
class 子类(父类一，父类二.....):
	pass
	
类名.__dict__       为类的所有属性
类名.__mro__        为类的方法解析顺序        MRO:Method Resolution Order
```